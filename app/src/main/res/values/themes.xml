<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ZabellaDriver" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/gradient_1</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="Theme.ZabellaDriver" parent="Base.Theme.ZabellaDriver" />

    <style name="PendingCalenderViewCustom" parent="Theme.AppCompat">
        <item name="colorAccent">@color/gradient_2</item>
        <item name="colorPrimary">@color/gradient_2</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
    </style>

</resources>